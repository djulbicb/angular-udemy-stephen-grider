import { ClassesDirective } from './classes.directive';

describe('ClassesDirective', () => {
  it('should create an instance', () => {
    const directive = new ClassesDirective();
    expect(directive).toBeTruthy();
  });
});
